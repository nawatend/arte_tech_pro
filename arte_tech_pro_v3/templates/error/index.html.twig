{% extends 'base_error.html.twig' %}

{% block title %}Error{% endblock %}

{% block body %}
    <div class="pos-a t-0 l-0 bgc-white w-100 h-100 d-f fxd-r fxw-w ai-c jc-c pos-r p-30">
        <div class="mR-60"><img alt="#" src="{{ asset('template/assets/static/images/logo_big.png') }}" ></div>
        <div class="d-f jc-c fxd-c"><h1 class="mB-30 fw-900 lh-1 c-red-500" style="font-size:60px">403</h1>
            <h3 class="mB-10 fsz-lg c-grey-900 tt-c">Oops Toegang geblokeerd</h3>

            {% if is_granted('ROLE_CLIENT') %}
                <p class="mB-30 fsz-def c-grey-700">Alleen toegankelijk voor Admin/Managers</p>
            {% endif %}

            {% if is_granted('ROLE_ADMIN') %}
                <p class="mB-30 fsz-def c-grey-700">Alleen toegankelijk voor klanten</p>
            {% endif %}

            <div><a href="{{ path('home') }}" type="primary" class="btn btn-primary">Go to Home</a></div>
        </div>
    </div>
{% endblock %}
